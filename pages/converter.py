import streamlit as st

conversion_factors = {
  'distance': ('mm':1), 
  'weight':('cm':0,1)
  'time':('m':0.01)}

'weight':pass,
'time':pass
}

#selection category
category = st.radio("Select category",options=conversion_factor.keys())
